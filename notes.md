# TODOs:
## Fixes/Optimisations:
|Priority    |Task                                                            |Description & Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                       |Impact                                                                                      |
|------------|----------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------|
|1 (Critical)|Persist HashChain across input chunks                           |Move HashChain into MCompressor struct (initialize once in new). Pass &mut hash_chain to process_lz77. Remove let mut hash_chain = HashChain::new(); inside process_lz77.   Current per-chunk reset prevents lookups into the full 32 KiB history on files >1 MiB, severely hurting ratio for repetitive data across chunk boundaries. The window already persists; the hash must too. The existing match_pos >= min_pos guard prevents invalid old positions.|Massive ratio improvement on large files Correctness (full LZ77 window utilization)         |
|2 (Critical)|Fix degenerate single-symbol alphabet                           |In get_huffman_tree, if frequency array has only one used symbol, force code length 1 (code 0). In map_canonical_codes_to_lz_symbols, avoid assigning length 0.   Length 0 currently writes nothing for that symbol → decoder cannot recover data if a block contains only one repeated literal/length code.                                                                                                                                                  |Prevents invalid/corrupt output in edge cases Correctness                                   |
|3 (High)    |Make Huffman priority queue deterministic                       |Implement custom Ord/PartialOrd for HuffmanTreeNode: compare primarily by weight ascending, tie-break by symbol (for leaves) or a unique node ID (add id: usize field, increment on creation). Remove derived Ord that compares pointers.   Current derived ordering is nondeterministic (pointer ties) → nondeterministic output for equal-weight nodes.                                                                                                     |Reproducible compression Cleanliness / reliability                                          |
|4 (High)    |Enforce maximum code length of 15 bits                          |Track depth during map_canonical_codes_to_lz_symbols. If any >15, either (a) reject/fallback to stored block or (b) implement length-limiting (simple post-process truncation or full Package-Merge algorithm).   Standard DEFLATE forbids >15; very skewed frequencies can produce longer codes → poor ratio and decoder incompatibility.                                                                                                                    |Ratio + robustness Step toward compliance                                                   |
|5 (High)    |Implement true canonical Huffman code assignment                |After tree traversal, collect (symbol, length) pairs. Sort by length ascending, then symbol ascending. Assign codes sequentially: start with code = 0, increment per symbol, left-shift when length increases. Store in maps as before.  Current traversal order is arbitrary → non-canonical codes. True canonical is required for standard compliance and simplifies decoder.                                                                               |Compliance readiness Cleaner decoder design                                                 |
|6 (High)    |Compress code-length header using standard DEFLATE format       |Implement HLIT (5 bits), HDIST (5 bits), HCLEN (4 bits). Define the fixed 19-symbol code-length alphabet order. Build Huffman tree for code lengths. Run-length encode the 317 code lengths (0 for unused, 16/17/18 repeats). Write compressed code lengths with the clen tree.  Current raw 317 × 8-bit dump wastes ~317 bytes per block. This is the single biggest ratio win available.                                                                    |Dramatic header size reduction → much better ratio Major step toward standard dynamic blocks|
|7 (Medium)  |Add proper block header bits (BFINAL + BTYPE)                   |Before each dynamic header, write 3 bits LSB-first: is_last ? 1 : 0 for BFINAL, then 0b10 for BTYPE=2 (dynamic). Use bit_writer.write_bits.                                                                                                                                                                                                                                                                                                                   |Full block framing Compliance                                                               |
|8 (Medium)  |Standardize block termination (use only 256 end-of-block symbol)|Remove manual Literal(256) push in process_lz77. Always increment lit_len_cnt[256] += 1 in put_lit_len_dist_freq for every block. After draining lz_symbols in process_huffman, always write the code for symbol 256. Remove special is_last EOS (286) handling entirely.  Aligns with RFC 1951: every block (including last) ends with 256; no separate EOS symbol.                                                                                          |Compliance + simpler logic                                                                  |
|9 (Medium)  |Remove zero-padding reliance / improve final flush              |After final block's 256, flush complete bytes only. Decoder can stop on 256, so explicit padding is unnecessary. Document exact bit length or rely on 256 termination.                                                                                                                                                                                                                                                                                        |Cleaner stream termination Decoder simplicity                                               |
|10 (Low)    |Implement matching decompressor                                 |Symmetric design: MDecompressor with BitReader, inverse Huffman (canonical lookup tables or tree), LZ77 output buffer. Start with raw custom format, then evolve with compliance fixes.                                                                                                                                                                                                                                                                       |Completeness (full compressor/decompressor library)                                         |
|11 (Low)    |Refactor for better block strategy                              |Make block size configurable or adaptive (e.g., new block when ratio estimate worsens). Consider fallback to stored or fixed blocks for uncompressible sections.                                                                                                                                                                                                                                                                                              |Further ratio gains More sophisticated semi-dynamic strategy                                |
## Tests
1. Switch to and fro vecdeque and vec in lz77 match finding (prev it was switched from vecdeque to vec).